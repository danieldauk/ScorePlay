import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#8691BD",
    },
    secondary: {
      main: "#EDF2FF",
    },
    text: {
      primary: "#686D90",
      secondary: "#BBBBDA",
    },
  },
  typography: {
    fontSize: 12,
  },
});
